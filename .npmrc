# Required for GitHub Packages authentication.
# Local dev: set NODE_AUTH_TOKEN in your shell or .env (do not commit tokens).
# CI: set NODE_AUTH_TOKEN as a repository secret.
@code-for-canada:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
